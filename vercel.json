{
  "installCommand": "npm ci --omit=dev",
  "rewrites": [
    {
      "source": "/api/v1/download/:bot_id/:file_id/:mime_base_type/:mime_sub_type/(.*)",
      "destination": "/api/v1/download"
    }
  ]
}
